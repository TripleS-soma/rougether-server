#!/bin/bash
set -euo pipefail
args=()
for arg in "$@"; do
  case "$arg" in
    -XX:MaxHeapSize=*|-Xmx*) args+=("-Xmx512m") ;;
    *) args+=("$arg") ;;
  esac
done
export JAVA_TOOL_OPTIONS="${JAVA_TOOL_OPTIONS:-} --enable-native-access=ALL-UNNAMED"
exec "${args[@]}"
