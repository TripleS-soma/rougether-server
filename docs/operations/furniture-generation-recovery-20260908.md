# 사진 가구 생성 장애 복구 — 2026-09-08

원본 사진 업로드가 실패하면 생성권은 반환되지만, 실패 작업이 일일 2회 제한에 포함되어 잔액이 있어도 새 요청이 거절됐습니다. `FAILED` 상태의 `SOURCE_UPLOAD_FAILED` 또는 `UPLOAD_INTERRUPTED` 중 AI 처리 시도가 모두 0인 작업만 일일 횟수에서 제외합니다. 기존 실패 기록에도 바로 적용되며 기록 삭제나 생성일 변경은 하지 않습니다. AI 처리를 시도한 실패와 성공 작업은 기존 한도를 유지합니다.

DEV에서는 768 MiB 컨테이너 제한을 넘겨 user-api가 OOM으로 종료됐습니다. 런타임에서 적용한 1280 MiB 제한을 배포 스크립트와 Packer/bootstrap 템플릿에도 반영합니다. 힙 상한은 512 MiB로 유지하며 RAM과 swap 합산 제한도 1280 MiB입니다. 후보 컨테이너 사전검사는 1280 MiB와 여유분 256 MiB를 합친 1536 MiB를 요구합니다.

사용자가 모든 활성 계정 보정을 명시적으로 승인하여 2026-09-08 07:47:25 UTC에 사용 가능 생성권을 각각 2회로 맞췄습니다. 적용 당시 활성 계정 104개 중 102개는 이미 2회였고, 2개를 보정하여 총 4회를 추가했습니다. 계정 1개를 새로 만들었고 예약 잔액은 전후 모두 0회였습니다. 커밋 후 재조회에서도 104개 모두 잔액 2회를 확인했습니다.

보정은 사용자와 계정을 잠근 단일 트랜잭션으로 적용했습니다. `repair-20260908-credits2`를 작업 식별자로 사용해 계정별 원장 104건을 남겼고, 재실행 시 이 기록이 있는 계정은 건너뜁니다. 보정 사유 `OPERATIONS_ADJUSTMENT`와 기존 출시 지급 사유 `PROMOTION_GRANT`를 Java enum에 반영합니다. 보정 전 백업은 DEV 서버의 `/var/lib/rougether/operations/repair-20260908-credits2-20260908T074725Z.json`에 제한된 권한으로 보관했습니다. 생성권 보정 자체가 일일 한도 오류의 코드 수정을 대신하지는 않습니다.
