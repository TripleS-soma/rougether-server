# 2026-09-08 생성권 지급과 출석 이벤트

사용자 요청에 따라 2026-09-08 활성 계정 105개(id <= 154)에 생성권 2회씩 추가 지급했다. 탈퇴 계정은 제외했으며 이후 가입자는 이번 일괄 지급 대상에 포함하지 않는다.

- 원장: `reference_id=launch-20260908-2`, `reason=PROMOTION_GRANT`
- 지급 결과: 105명, 총 210회. 재실행 추가 지급 0명, 새 연결 재조회에서 누락 0명.
- 운영 이벤트: `AI_FURNITURE_20260908`, id 1, KST 2026-09-08~2026-09-28(양끝 포함 21일).
- 수동 7일 연속 출석. 매일 30코인, 5일차 50코인, 완주 생성권 1회.
- 완주는 계정당 이 이벤트에서 한 번만 가능하다. 완료된 최신 출석 기록이 있으면 서버가 이후 체크인을 멱등 응답으로 종료하며 추가 코인·생성권을 지급하지 않는다.
- 일괄 지급은 출석 보상과 별개이며 기존 생성권 잔액에 더한다. 예약 잔액은 건드리지 않는다.

`deploy/sql/attendance-launch-20260908.sql`은 실제 실행한 운영 SQL이다. 캠페인 잠금과 서버와 같은 사용자 행 잠금을 사용하고 지급 원장·잔액·이벤트를 한 트랜잭션으로 처리한다. 두 번째 호출은 중복 지급 방지 검증이다. 임시 프로시저는 실행 후 삭제한다.

앱은 부팅 때 출석 이벤트를 조회한다. 이벤트 등록 전에 실행 중이던 앱은 완전히 종료 후 다시 열어야 `내 정보`의 출석 메뉴와 AI 가구 화면의 출석 안내가 표시된다.

운영 서버는 2026-09-08 `5e5c6470`을 블루·그린 방식으로 배포했다. `ROUGETHER_DEPLOY_MODE=hold`는 유지했다. 이번 추가 원장 유형은 enum 등록이며 현재 배포된 사용자 API는 프로모션 원장 행을 역직렬화하는 경로를 사용하지 않는다.
