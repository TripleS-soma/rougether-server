spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

# 별도 클라우드 자원 없이 요청 스레드에서 활동 기록을 묶어 커밋함.
activity:
  batch:
    enabled: ${ACTIVITY_BATCH_ENABLED:true}
